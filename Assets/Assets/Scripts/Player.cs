using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
    public bool canMove;
    public GameObject[] players;

    // Update is called once per frame
    void Update()
    {
    //     if (!canMove) return;
        
    //     for(int i = 0; i < Input.touchCount; i++)
    //     {
    //         foreach(var player in players){

    //         Vector2 mousPos = Camera.main.ScreenToWorldPoint(Input.GetTouch(i).position);
    //         Debug.Log(mousPos);
    //         if (!player.GetComponent<CircleCollider2D>().OverlapPoint(mousPos))
    //             return;
    //         player.GetComponent<Rigidbody2D>().MovePosition(mousPos);
    //         }
        
    //     }
    }
        
    }

